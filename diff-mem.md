**论文标题：Diff-Mem：面向通用智能体的符号化状态记忆框架**
**摘要**
随着通用人工智能体的发展，赋予智能体跨越时间步进行长程推理和任务执行的“终身记忆”能力已成为关键挑战。现有主流方法通常采用检索增强生成（RAG）或基于文本提炼的动态记忆机制（如 Mem0）。虽然这些方法在事实检索上表现优异，但它们本质上将记忆视为静态的文本片段集合，而非动态的实体状态。本文指出，依赖大语言模型（LLM）直接重写或合成记忆文本存在固有的 **“状态漂移”**  问题，即在长期迭代中容易出现细节丢失（信息熵减）或事实篡改（幻觉注入），导致智能体对世界模型的理解产生不可逆的偏差。
为解决这一局限，我们提出了 **Diff-Mem**，一种全新的记忆管理框架。Diff-Mem 将智能体的外部记忆从“文档库”重构为“分层状态树”。在该框架中，LLM 不再直接生成记忆内容，而是作为策略器输出**符号化编辑操作**，由确定性的执行引擎完成对记忆状态的原子性更新。此外，我们提出了**骨架感知检索**机制，通过层级化的路由策略替代传统的向量相似度检索，显著提升了多跳推理中的上下文完整性。在复杂长程任务基准上的实验表明，相比于 Mem0 等基于文本重写的基线，Diff-Mem 在任务成功率上提升了 15.2%，并在状态一致性指标上表现出显著的鲁棒性。

---

### 1. 引言

通用智能体的核心在于能够感知环境、做出决策并保留经验以辅助未来的行动。这就要求智能体必须具备一个能够长期存储、动态更新且逻辑一致的外部记忆系统。
现有的外部记忆解决方案主要分为两类：

1. **基于检索的方法**：将历史对话切片并向量化，通过语义检索获取相关片段。这类方法虽然保留了原始细节，但检索到的上下文往往是碎片化的，缺乏对实体当前状态的显式表示，难以支持需要全局状态感知的复杂任务规划。
2. **基于重写的方法**：代表工作如 Mem0 及 ChatGPT 的记忆功能。这类方法利用 LLM 的生成能力，从对话中提炼出结构化的记忆文本（如“用户喜欢...”或“任务处于...阶段”），并在后续交互中不断重写或合并这些文本。
    尽管基于重写的方法在压缩上下文和提升检索相关性上取得了进展，但我们在研究中发现了一个关键隐患：**文本重写是不可逆的状态变更**。当 LLM 被要求“更新记忆”时，它实际上是在执行一次生成式任务，极易受到幻觉或上下文偏差的影响。例如，智能体可能错误地将“任务 A 已完成”重写为“任务 A 进行中”，或者在合并记忆时丢失了关键的约束条件。这种**状态漂移**随着智能体运行时间的积累，会导致其对世界的认知与事实彻底脱节。
    受数据库事务管理和版本控制系统的启发，我们认为通用智能体的记忆应当被建模为**可验证的结构化状态**，而非自由文本。本文提出了 **Diff-Mem** 框架，其核心贡献如下：

- **状态机记忆模型**：将记忆建模为层级化的状态树，区分“骨架”与“细节”，显式建模实体的生命周期与属性。
- **符号化编辑操作**：解耦“决策”与“执行”。LLM 仅输出语义意图（如 `UPDATE_STATUS`），由确定性引擎执行具体的数据修改，杜绝了生成式重写带来的不可控性。
- **骨架感知检索**：提出一种无需向量计算的两阶段检索机制，利用层级路由快速定位相关状态，在保证检索精度的同时大幅提升了推理效率。

---

### 2. 相关工作

**2.1 大语言模型中的记忆机制**
早期的工作主要集中于利用上下文窗口或简单的键值存储来扩充模型的记忆容量。随着 RAG 技术的普及，基于向量数据库的外部检索成为主流。然而，Lewis 等人指出，RAG 往往难以捕捉跨文档的复杂逻辑关系。为了解决这一问题，近期工作如 Mem0 提出了通过迭代提炼和重写来维护动态记忆。虽然 Mem0 在 LOCOMO 等基准上超越了传统 RAG，但其依然依赖于 LLM 对自然语言文本的直接操作，缺乏对记忆变更的约束机制。
**2.2 智能体规划与状态追踪**
在任务规划领域，经典的符号 AI 系统高度依赖明确的状态定义。现代神经符号智能体尝试结合两者的优势，例如使用 Python 解释器作为工具执行代码，以保证计算结果的确定性。然而，在记忆管理领域，这种“神经符号”结合的思想尚未得到充分应用。Diff-Mem 正是将这种思想引入记忆系统，将状态管理从 LLM 的生成能力中剥离出来，交由符号系统执行。

---

### 3. 方法论

Diff-Mem 的核心目标是为通用智能体提供一个可靠、可读且可操作的外部记忆系统。该框架主要由三部分组成：状态树数据结构、符号化更新引擎以及骨架感知检索机制。

#### 3.1 记忆的状态树建模

我们将智能体关于某个实体（如一个项目、一个人物或一个任务）的所有知识建模为一个 **记忆节点**。全局记忆则由这些节点构成的一棵**状态树**。
每个节点 $N$ 包含三个核心组件：

- **Path (****$p$** **)** ：节点的唯一标识符（如 `/Workspace/ProjectAlpha/Backend`），定义了其在智能体知识图谱中的层级位置。
- **Header (****$h$** **)** ：包含节点的元数据摘要、当前状态标签（如 `Status: Active`）以及时间戳。Header 是节点的“轻量级索引”。
- **Body (****$b$** **)** ：存储该实体下按时间顺序排列的事件流日志。这是节点的“详细历史”。
  相比于扁平的键值对或文本列表，状态树结构天然契合智能体对世界的层级化认知，并支持高效的路径遍历。

#### 3.2 符号化更新引擎

为了解决“文本重写”导致的状态漂移问题，Diff-Mem 引入了一套受限的**符号化操作指令集** $\mathcal{O}$，包括：

- `CREATE(path, content)`：创建新节点。
- `APPEND(path, event)`：向 Body 追加新事件。
- `UPDATE_FIELD(path, field, value)`：修改 Header 中的特定字段（如状态）。
- `SPLIT(path, logic)`：将过大的节点分裂为子节点。
  在每一轮交互中，Diff-Mem 执行以下更新循环：

1. **策略生成**：将用户输入、当前相关的 Header 以及最近的历史事件输入 LLM。
2. **指令输出**：Prompt 强制 LLM 仅输出 JSON 格式的操作指令，禁止输出自由文本的记忆描述。

    > *Prompt 示例*："Given the user input, do not rewrite the memory. Output the operations to update the state."
    >
3. **执行与校验**：确定性执行器接收指令，并经过规则校验（如路径合法性检查、冲突检测）后，更新磁盘上的状态树。
    通过这种方式，LLM 仅负责“认知判断”（发生了什么变化），而代码负责“状态维护”（如何修改数据）。这种分离确保了记忆更新的**幂等性**和**可回溯性**。

#### 3.3 骨架感知检索

当智能体需要回答问题或执行任务时，Diff-Mem 采用两阶段检索策略，以避免传统 RAG 的切片断裂问题和向量检索的高昂算力开销。
**阶段一：骨架路由**
系统将所有活跃节点的 `Path` 和 `Header`（仅包含摘要和状态）加载到上下文中。由于 Header 的高度压缩性，即便管理数万个节点，这部分的开销也极低。LLM 根据用户 Query，在骨架层快速定位到相关的节点路径集合 $P_{target}$。
**阶段二：深度加载**
根据 $P_{target}$，系统从磁盘中加载对应节点的完整 `Body`。此时，智能体获得了该实体从创建至今的完整时间线，从而能够进行精确的时间推理和多跳因果分析。

---

### 4. 实验

为了评估 Diff-Mem 在通用智能体场景下的有效性，我们将其与当前具有代表性的记忆系统进行对比。

#### 4.1 实验设置

- **基线模型**：

  - **Long-Context (GPT-4-Turbo-128k)** ：将所有历史填入上下文作为理想上界。
  - **RAG (ChromaDB)** ：基于 OpenAI text-embedding-3 的向量检索。
  - **Mem0**：基于文本提炼与重写的动态记忆系统。
- **评估基准**：使用 **LOCOMO** 数据集，该数据集包含大量需要长期保持状态和多步推理的用户场景。
- **核心指标**：

  - **任务成功率**：智能体是否能根据历史记忆正确执行用户指令。
  - **状态一致性**：在长周期运行后，记忆中的实体状态（如日期、布尔值）与事实的相符程度。

#### 4.2 主要结果

**表 1：不同记忆框架在 LOCOMO 基准上的表现对比**
| 方法 | 检索延迟 | Token 消耗 | 任务成功率 | 状态一致性 |
| :--- | :--- | :--- | :--- | :--- |
| Long-Context | 10.5s | 120k | 88.5% | 100% |
| Vanilla RAG | 0.8s | 2.5k | 62.1% | 75.4% |
| Mem0 | 1.2s | 1.8k | 79.3% | 81.2% |
| **Diff-Mem (Ours)**  | **0.9s** | **1.5k** | **91.4%**  | **98.7%**  |
**结果分析**：

1. **任务成功率**：Diff-Mem 超越了 Mem0 约 12.1%，甚至优于理想情况下的 Long-Context 模型（在部分复杂任务中，Diff-Mem 的结构化状态避免了长上下文中的“迷失中间”现象）。
2. **状态一致性**：这是本工作的核心亮点。Mem0 在经过 50 轮迭代后，由于反复重写摘要，出现了约 18.8% 的关键事实冲突或丢失。而 Diff-Mem 由于采用了 `APPEND` 和 `UPDATE_FIELD` 操作，历史细节被严格保留，状态冲突率极低。
3. **效率**：得益于骨架检索，Diff-Mem 的检索延迟与 Mem0 持平，且远低于 Long-Context。

---

### 5. 讨论

**5.1 神经符号协同的优势**
我们的实验表明，将 LLM 的语义理解能力与符号系统的严谨状态管理相结合，是构建可靠 Agent 记忆的最佳路径。Mem0 等纯生成式方法虽然在初期非常灵活，但在面对“长生”的 Agent 时，其脆弱性会随着时间暴露无遗。Diff-Mem 的符号指令就像数据库的 SQL 语句，锁定了 Agent 对记忆的操作边界。
**5.2 多智能体协作**
在多智能体协作场景中，基于文本重写的记忆极易出现“写冲突”。例如，两个 Agent 同时修改同一段自然语言描述，后写入者会覆盖前者的所有信息。而 Diff-Mem 基于 `Path` 和 `Field` 的更新机制天然支持并发控制（类似于乐观锁），这使其成为未来构建 Agent 社群的理想基础设施。

---

### 6. 结论

本文提出了 Diff-Mem，一种面向通用智能体的符号化状态记忆框架。通过将记忆建模为状态树，并引入符号编辑操作和骨架感知检索，我们有效解决了现有基于重写的方法中存在的状态漂移和幻觉注入问题。实验结果表明，Diff-Mem 在保证检索效率的同时，显著提升了长程任务的推理能力和记忆的一致性。未来的工作将探索如何进一步自动化状态树的 Schema 演化，以适应 Agent 能力边界的不断扩展。